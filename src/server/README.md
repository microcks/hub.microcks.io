## Start the server

Install the dependencies:
```bash
npm install
```

Run the server:
```bash
npm start
```

You can specify the port to listen on with the `PORT` environment variable:
```bash
PORT=4001 npm start
```